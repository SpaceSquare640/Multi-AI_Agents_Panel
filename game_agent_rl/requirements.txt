# Track B (Deep RL) offline tooling — see Game-Playing Agent Design.md.
# Not bundled into installers (unlike ml/requirements.txt's
# sentence-transformers — see ADR 0003 for that decision): this is
# developer/research tooling run manually to produce training data and
# checkpoints, not a capability the packaged app calls at runtime.
mss>=9.0
pynput>=1.7
