[package]
name = "multi-ai-agents-panel"
version = "0.1.83-alpha"
description = "A desktop panel for running and orchestrating multiple local and cloud AI agents"
authors = []
license = "MIT"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "map_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
keyring = { version = "3", features = ["windows-native", "apple-native", "sync-secret-service"] }
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
# game_agent (Track A, see Game-Playing Agent Design.md): screen capture +
# mouse/keyboard automation for the LLM vision loop, plus base64 for
# encoding screenshots into Ollama's `images` field.
xcap = "0.9"
enigo = "0.2"
base64 = "0.22"
image = { version = "0.25", default-features = false, features = ["png"] }

