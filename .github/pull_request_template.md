## What does this change do, and why?

<!-- Link related issues with "Fixes #123" if applicable. -->

## How was this verified?

<!--
This project's standing bar (see CONTRIBUTING.md): cargo test, npm run test,
npm run build must all pass, and cargo clippy --all-targets -- -D warnings
must be clean. State plainly which of these you ran, and be honest about
anything you couldn't verify (e.g. no live provider API key, no real
Ollama/colibrì instance available) rather than implying full coverage.
-->

## Checklist

- [ ] `cargo test` passes
- [ ] `cargo clippy --all-targets -- -D warnings` is clean
- [ ] `npm run test` passes
- [ ] `npm run build` passes
- [ ] No secrets (API keys, tokens) in the diff
